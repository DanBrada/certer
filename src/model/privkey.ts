export function retrievePrivateKey() {

}

export function privateKeyExists() :boolean {
    return false
}

export function createPrivateKey(length: number) {
    // TODO: interface with WASM to generate the public key
}